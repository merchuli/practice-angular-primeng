import { CountryInfoPipe } from './country-info.pipe';

describe('CountryInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
